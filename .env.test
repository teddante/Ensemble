# Test environment variables for E2E testing
# These are not real credentials - used only for test mocking

# Required for cookie encryption during tests (must be at least 32 characters)
COOKIE_ENCRYPTION_KEY=e2e-test-encryption-key-32-chars-or-more-here

# OpenRouter API Key not needed - we mock all API calls
# OPENROUTER_API_KEY=not-needed-for-e2e-tests
